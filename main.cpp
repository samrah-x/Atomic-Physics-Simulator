//#include "raylib.h"
#include <iostream>
#include "button.hpp"
#include "game.hpp"

int main() {
    init();        // Initialize the game
    runGame();     // Run the game loop
    CloseWindow(); // Close the game window
    return 0;
}
